（1）生成器
通过一定算法，生成出一系列的值。边生成边运算，达到节约资源的目的。
a.生成器在调运的时候，才生成相应的数据
b.生成器只记录当前的位置，只有一个方法__next__()
c.生成器当元素打印结束后就停止运行

list = [1,2,3,4,5,6,7,...,10000]

用途："协程"的效果----"单线程下的并行效果"  | 一步io的雏形（nginx）
“生产者---消费者模型：”kafka，

yield b    #保存了函数的中断状态，并返回

对于生成器的对象：
c.send("masage")  #唤醒生成器，执行
c.__next__()    #唤醒生成器，不执行
